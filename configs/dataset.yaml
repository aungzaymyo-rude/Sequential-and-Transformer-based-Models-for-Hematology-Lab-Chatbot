seed: 42
results_dir: results/metrics

data:
  labeled_dir: data/labeled
  processed_dataset_path: data/processed/intent_dataset.jsonl
  splits_dir: data/splits
  test_size: 0.15
  val_size: 0.15
  language: en

preprocessing:
  lowercase: true
  strip_control_chars: true
  collapse_whitespace: true
